# Tools

This directory is reserved for release and validation scripts.

Planned:

- `package-plugin.sh`: build a Moodle-ready `webirr.zip` from `plugin/webirr`.
- `run-moodle-demo.sh`: start the Moodle checkout example site.
- `capture-screenshot.sh`: capture the checkout screenshot from the real Moodle
  plugin UI once the Moodle example site is codified.
